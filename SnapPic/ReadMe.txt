主要功能是把sensor画面输出到panel显示并保存yuv420图像到当前目录

编译方法：
1.将SnapPic文件夹放到project同一级目录;
2.进入到SnapPic目录，make clean；make即可产生执行档：SnapPic

抓图保存的方法：
输入字母j/J + 回车，抓取MJPG；输入字母y/Y + 回车，抓取NV12。

用法如下：
./SnapPic

